your-domain.com {
    reverse_proxy whats-if:8000
    header /static/* Cache-Control "public, max-age=3600"
}

# For local dev / LAN without domain — uncomment below and comment above:
# :80 {
#     reverse_proxy whats-if:8000
#     header /static/* Cache-Control "public, max-age=3600"
# }
